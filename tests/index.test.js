import { resolve } from "path";

import test from "ava";

import { loadConfigSync } from "../lib";

test("successfully loads config with both params specified", async t => {
  t.true(true);
});

test("correctly infers root directory when not specified", async t => {
  t.true(true);
});

test("correctly infers config path when not specified but root dir specified", async t => {
  t.true(true);
});
